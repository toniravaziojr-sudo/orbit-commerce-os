# Memory: features/marketing/ads-chat-autonomous-governance-v5-24
Updated: 2026-03-11

O Ads Chat (v5.35.0) utiliza uma arquitetura de dados em tempo real e execução rigorosa. 1. Fonte de Dados: Prioriza consulta via Meta Graph API (fetchMetaCampaignsLive) com paginação total. Default `date_preset=maximum` (LIFETIME). Banco local é fallback. 2. Conversões — MAX Value Deduplication: Itera 8 action_types de purchase, usa o MAIOR valor. NÃO soma tipos. 3. tool_choice Inteligente (v5.35.0): A chamada inicial usa `tool_choice="required"` para mensagens que pedem dados (targeting, performance, campanhas, "tente novamente") e `"auto"` para perguntas gerais. Corrige o problema onde Gemini Flash escolhia gerar texto em vez de chamar ferramentas. 4. Sanitização de Histórico (v5.35.0): Remove mensagens filler do histórico antes de enviar ao modelo, prevenindo "efeito espelho" onde o modelo replica padrões de filler anteriores. 5. Anti-Filler DUAL com Fallback Multi-Provedor (v5.35.0): 24+ padrões em DOIS pontos de detecção. Ao detectar filler: (a) faz retry com mensagens limpas (system + última msg + instrução direta), (b) se Gemini falhar, tenta OpenAI gpt-5-mini como fallback, (c) se ambos falharem, retorna erro honesto — NUNCA envia filler ao usuário. O retry usa TransformStream para manter o indicador de carregamento ativo. 6. Feedback: Eventos SSE de progresso. 7. Integridade: Nomes exatos, proibindo abreviações. 8. Imagens: Ctrl+V para colar screenshots. 9. Targeting — Sync & Cache: `fetchMetaAdsetsLive` faz paginação COMPLETA (até 500 adsets). `getAdsetTargeting` busca até 20 adsets com retry em 429. Cache via `syncAdsetToCache()`. 10. Fluxo de Targeting: Passo 1 `get_meta_adsets` (sem live). Passo 2 `get_adset_targeting` com IDs.
